'use strict';

/**
 * Angular ng-docs
 */

module.exports = {
    options: {
        title: '<%= meta.pkg.name %>'
    },
    all: {
        src: ['<%= meta.src %>/app/**/*.js'],
        title: 'Documentation'
    }
};
